apiVersion: v1
kind: Service
metadata:
  name: wms-system-service
  namespace: wms
spec:
  ports:
    - name: http
      port: 8082
      targetPort: 8082
  selector:
    app: wms-system-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wms-system-service
  namespace: wms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wms-system-service
  template:
    metadata:
      labels:
        app: wms-system-service
    spec:
      initContainers:
        - name: wait-for-mysql
          image: mysql:8.0
          command:
            - sh
            - -c
            - until mysqladmin ping -hmysql -uwms -p"$MYSQL_PASSWORD" --silent; do sleep 2; done
          env:
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: wms-mysql-secret
                  key: password
      containers:
        - name: wms-system-service
          image: wms/wms-system-service:0.0.1
          imagePullPolicy: IfNotPresent
          env:
            - name: DB_URL
              value: jdbc:mysql://mysql:3306/wms_cloud?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: wms-mysql-secret
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: wms-mysql-secret
                  key: password
            - name: EUREKA_CLIENT_ENABLED
              value: "false"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: http://wms-discovery:8761/eureka/
            - name: EUREKA_INSTANCE_HOSTNAME
              value: wms-system-service
            - name: EUREKA_INSTANCE_PREFER_IP_ADDRESS
              value: "false"
          ports:
            - name: http
              containerPort: 8082
